<hr>

<div class="row pt-3 mb-4">
  <div class="col-md-12">
    <button id="btnOralHealthRecord" class="btn btn-outline-primary" type="button" data-toggle="collapse" data-target="#divOralHealthRecord" aria-expanded="false" aria-controls="divOralHealthRecord">
      <i class="nav-icon fas fa-plus"></i>&nbsp;&nbsp;
      Oral Health Records
    </button>
  </div>
</div>

<div id="divOralHealthRecord" class="collapse">
  <div class="row">
    <div class="col-md-12">
      <!-- general form elements -->
      <div class="card card-primary">
        <div class="card-header">
          <h3 class="card-title text-capitalize">Oral Health Records</h3>
        </div>
        <!-- /.card-header -->
        <% if (userData.status === 'Missed') { %>
          <div class="card-body">
            <label>Not Recorded</label>
          </div>
        <% } if (userData.status !== 'Missed'){ %>
        <div class="card-body">
          <div class="row">
            <div class="col-md-12">
              <div class="form-group">
                <label for="tb_recordTeeth">Tooth(s) Recorded</label>
                <input type="text" class="form-control-plaintext" name="recordTeeth" value="<%= oralrecordData.recordTeeth ? oralrecordData.recordTeeth : 'Not provided' %>" disabled>
              </div>
              <div class="form-group">
                <label for="tb_TeethDesc">Teeth Description</label>
                <textarea class="form-control-plaintext" name="recordDescription" disabled><%= oralrecordData.recordDescription ? oralrecordData.recordDescription : 'Not provided' %></textarea>
              </div>
              <div class="form-group">
                <label for="tb_CreatedAt">Created At</label>
                <input type="text" class="form-control-plaintext" name="recordCreatedAt" value="<%= oralrecordData.recordCreatedAt ? oralrecordData.recordCreatedAt : 'Not provided' %>" disabled>
              </div>            
            </div>
          </div>
        </div>           
        <% } %>
        <!-- /.card-body -->
      </div>
      <!-- /.card -->
    </div>
  </div>
  <!-- /.row -->
</div>

<!-- jQuery -->
<script src="/plugins/jquery/jquery.min.js"></script>

<script>
  $('#btnPatientDetail').click(function() {
    $(this).toggleClass('btn-outline-primary').toggleClass('btn-primary');
    $(this).find('i').toggleClass('fa-plus').toggleClass('fa-minus');
  });

  $('#btnOralHealthRecord').click(function() {
    $(this).toggleClass('btn-outline-primary').toggleClass('btn-primary');
    $(this).find('i').toggleClass('fa-plus').toggleClass('fa-minus');
  });
</script>